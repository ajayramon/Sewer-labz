import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SEWER LABZ - Professional Sewer Inspection Reports",
  description: "Create professional sewer inspection reports with video documentation, defect tracking, and PDF export. SEWER LABZ simplifies sewer line inspections.",
  keywords: "sewer inspection, inspection reports, PDF reports, sewer line, CCTV inspection",
  authors: [{ name: "SEWER LABZ" }],
  creator: "SEWER LABZ",
  robots: "index, follow",
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 5,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
